/**
 * Used as interface ("implements"). Defines static members for Schema/Model interaction with mongoose
 */
var MongooseSchemaBuilder = (function () {
    function MongooseSchemaBuilder() {
    }
    return MongooseSchemaBuilder;
})();
exports.MongooseSchemaBuilder = MongooseSchemaBuilder;
